import revertByWord from '../../code/string/lesson1'

test('revertByWord: Let\'s take LeeCode contest', () => {
  expect(revertByWord("Let's take LeeCode contest")).toBe("s'teL ekat edoCeeL tsetnoc")
})
